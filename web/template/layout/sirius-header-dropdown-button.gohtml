{{ define "header-dropdown-button" }}
  <details class="panel-tabs__details" name="sirius-header-dropdown">
    <summary id="header-button-{{ replace (ToLower .Label) " " "-" }}"
       class="panel-tabs__button panel-tabs__summary"
       aria-label="{{ .Label }}"
       aria-controls="panel-tabs__dropdown-{{ replace (ToLower .Label) " " "-" }}"
       data-label="{{ .Label }}"
    >
      <svg aria-hidden="true" focusable="false" class="page-header__button-icon">
        <use href="{{ prefixSVG .IconName }}" />
      </svg>
      <span class="govuk-visually-hidden">{{ .Label }}</span>
    </summary>

    <div class="panel-tabs__dropdown">
      <div class="govuk-width-container">
        <div id="panel-tabs__dropdown-{{ replace (ToLower .Label) " " "-" }}"
             class="page-header__dropdown"
             role="region"
             aria-labelledby="header-button-{{ replace (ToLower .Label) " " "-" }}"
             hx-get="{{ .URL }}"
             hx-trigger="toggle[event.target.open] from:closest details once"
             hx-swap="innerHTML"
        ></div>
      </div>
    </div>
  </details>
{{ end }}